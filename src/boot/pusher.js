// import Pusher from 'pusher-js'

// Pusher.logToConsole = true;

// var pusher = new Pusher(process.env.PUSHER_KEY, {
//   cluster: 'eu',
//   encrypted: true,
// });
//     var channel = pusher.subscribe('user');
//     var channelT = pusher.subscribe('transaction')

// export {channel, channelT}